# Dependencies

fabric_kotlin_version=1.13.13

modmenu_version=11.0.0-rc.2

# Dev Dependencies

debug_utils_version=1.0.9

# Datagen Dependencies
# Only on the 'datagen' source set. Never compiled into or shipped with the mod.
#
# Modrinth version IDs, not version numbers.
# https://api.modrinth.com/v2/project/<slug>/version?loaders=["fabric"]&game_versions=["1.21.1"]

hybrid_aquatic_version=F5POkJG0

# Hard dependencies of the above.

geckolib_version=3GjkJptS
biolith_version=aBinwigO
trinkets_version=JagCscwi
lithostitched_version=uGT59zm8

# Trinkets' own dependency, pulled from the Ladysnake maven as separate modules because the
# Modrinth bundle's nested jars are not unpacked into the dev run.

cardinal_components_version=6.1.3
